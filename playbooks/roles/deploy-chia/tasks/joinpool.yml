# join HPool
- name: "join hpool to mine XCH :-)"
  shell: "(nohub sh ~/{{ inventory_hostname }}/chia_join_hpool.sh -config {{ inventory_hostname }}/hpool_conf.yml &)"
  async: 2592000               # 60*60*24*30 – 1 month
  poll: 0