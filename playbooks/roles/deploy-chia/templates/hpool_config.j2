path:
- /media/{{ PARAM }}/data1
- /media/{{ PARAM }}/data2
minerName: "{{ hostvars[inventory_hostname].group_names[0] }}-{{ inventory_hostname }}"
apiKey: "{{ hostvars[inventory_hostname].apikey }}"
cachePath: ""
deviceId: "{{ inventory_hostname }}"
extraParams: {}
log:
  lv: info
  path: ./log/
  name: miner-{{ hostvars[inventory_hostname].group_names[0] }}-{{ inventory_hostname }}"
url:
  info: ""
  submit: ""
scanPath: true
scanMinute: 60
