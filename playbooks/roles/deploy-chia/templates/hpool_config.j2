path:
- /media/ray/data1
- /media/ray/data2
minerName: "{{ hostvars[inventory_hostname].group_names[0] }}-{{ inventory_hostname }}"
apiKey: "{{ hostvars[inventory_hostname].apikey }}"
cachePath: ""
deviceId: "{{ inventory_hostname }}"
extraParams: {}
log:
  lv: info
  path: ./log/
  name: miner-{{ hostvars[inventory_hostname].group_names[0] }}.log
url:
  info: ""
  submit: ""
scanPath: true
scanMinute: 60
